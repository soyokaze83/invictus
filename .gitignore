# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, created when using `go test -local`
*.out

# Dependency directories (rarely used in Go, but kept for safety)
vendor/

# Personal/Tooling
.env
tmp